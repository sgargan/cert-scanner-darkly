{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Cert Scanner",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "main/main.go",
            "args": [
                "-d",
                "-c", "${workspaceFolder}/example/config.yaml"
            ]
        }
    ]
}